Written a recursive function which takes three arguments: `suffix`, `path`, `files`.
And appending to the list as when finding the `.c` file.

Time complexity: O(depth * Number of directories in each level)
Space complexity: O(depth)